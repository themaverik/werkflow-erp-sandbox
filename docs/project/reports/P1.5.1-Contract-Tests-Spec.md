# P1.5.1 Contract Tests Specification

**Goal**: Write unit tests (mock-based) for core domain service contracts without testing entities or HTTP layers.

**Scope**: 4 services, 3-5 tests each, ~16-20 total tests, ~8 hours

---

## EmployeeService Contract Tests

**Location**: `services/business/src/test/java/com/werkflow/business/hr/service/EmployeeServiceTest.java`

**Key Contracts to Test:**

1. **Duplicate Email Validation**
   - Create employee with email that already exists  (calls) throws `IllegalArgumentException`
   - Message contains "Email already exists"

2. **Duplicate Keycloak User ID Validation**
   - Create employee with keycloakUserId already linked  (calls) throws `IllegalArgumentException`
   - Message contains "Keycloak user already linked"

3. **Link Keycloak User - Idempotent**
   - Link same keycloakUserId twice  (calls) both succeed (no error)
   - Contract: linking is idempotent

4. **Link Different Keycloak User - Conflict**
   - Employee already linked to keycloakUserId A
   - Try to link to keycloakUserId B  (calls) throws `DataIntegrityViolationException`
   - Message contains "already linked to a different Keycloak user"

5. **Department Head Uniqueness**
   - Dept head (doaLevel=2) already exists for department code "SALES"
   - Try to create new dept head for same dept  (calls) throws `IllegalStateException`
   - Message contains "department head already exists"

6. **Tenant Isolation**
   - Employee exists but belongs to different tenant
   - Try to access via current tenant context  (calls) throws `EntityNotFoundException`
   - Contract: no cross-tenant data leakage

---

## BudgetCheckService Contract Tests

**Location**: `services/business/src/test/java/com/werkflow/business/finance/service/BudgetCheckServiceTest.java`

**Key Contracts to Test:**

1. **No Budget Plan Found**
   - No budget plan for department in fiscal year
   - Response: `available=false`, reason contains "No budget plan found"
   - `availableAmount=0`, `allocatedAmount=0`

2. **Sufficient Budget**
   - Allocated: 100,000, Spent: 20,000, Requested: 50,000
   - Available: 80,000 (allocated - spent)
   - Response: `available=true`, availableAmount=80,000

3. **Insufficient Budget**
   - Allocated: 50,000, Spent: 40,000, Available: 10,000
   - Requested: 15,000
   - Response: `available=false`, reason contains both amounts

4. **Fiscal Year Default**
   - No fiscal year in request  (calls) defaults to current year
   - Response contains current year

5. **Exact Match**
   - Allocated: 100,000, Spent: 70,000, Available: 30,000
   - Requested: 30,000
   - Response: `available=true`

6. **Zero Allocated**
   - Allocated: 0, Spent: 0, Requested: 1,000
   - Response: `available=false`

---

## PurchaseRequestService Contract Tests

**Location**: `services/business/src/test/java/com/werkflow/business/procurement/service/PurchaseRequestServiceTest.java`

**Key Contracts to Test:**

1. **Invalid Department Validation**
   - Create PR with invalid departmentId
   - Validator throws `EntityNotFoundException`  (calls) propagates

2. **Create PR - Success**
   - Valid department, all fields set
   - Response: id, prNumber, status=DRAFT, totalAmount=0
   - PR number follows pattern: `PR-{TENANT_ID}-{YEAR}-{SEQ:05d}`

3. **Default Priority**
   - No priority in request  (calls) defaults to `MEDIUM`

4. **Tenant Isolation - Get**
   - PR exists but belongs to different tenant
   - GetById  (calls) throws `AccessDeniedException`

5. **Tenant Isolation - Delete**
   - Try to delete PR from different tenant
   -  (calls) throws `AccessDeniedException`

6. **Update with New Department**
   - Update PR, change to new department
   - Validator called to validate new department exists

7. **ProcessInstanceId Preservation**
   - Create PR with processInstanceId
   - Response contains same processInstanceId

---

## AssetRequestService Contract Tests

**Location**: `services/business/src/test/java/com/werkflow/business/inventory/service/AssetRequestServiceTest.java`

**Key Contracts to Test:**

1. **Default Status**
   - Create request without explicit status
   - Status defaults to `PENDING`

2. **Approve Request**
   - Approve request by processInstanceId and approver user ID
   - Status changes to `APPROVED`
   - Response contains approverUserId

3. **Reject Request**
   - Reject request with rejection reason
   - Status changes to `REJECTED`
   - Response contains reason and approverUserId

4. **Non-Existent Request**
   - GetById(999) where 999 doesn't exist
   -  (calls) throws `EntityNotFoundException`, message contains "AssetRequest not found"

5. **Update ProcessInstanceId**
   - Update request with processInstanceId
   - Response contains updated processInstanceId

6. **Initiate Procurement**
   - Approved request, initiate procurement
   - Status changes to `PROCUREMENT_INITIATED`

---

## Testing Approach

**Use Mockito mocks for all dependencies:**
- Repositories  (calls) use `when().thenReturn()`
- TenantContext  (calls) return fixed tenant ID
- Validators  (calls) throw exceptions as needed

**No entity builders needed:**
- Create simple objects with all fields set directly
- Focus on service contract, not entity construction

**Pattern:**
```java
@Mock
private SomeRepository repo;

@Mock
private TenantContext tenantContext;

private ServiceUnderTest service;

@BeforeEach
void setUp() {
    MockitoAnnotations.openMocks(this);
    service = new ServiceUnderTest(repo, tenantContext);
    when(tenantContext.getTenantId()).thenReturn("ACME");
}

@Test
void testContractBehavior() {
    // Arrange: mock repository calls
    when(repo.findById(1L)).thenReturn(Optional.of(mockObject));

    // Act: call service
    ResultType result = service.operation(input);

    // Assert: verify contract
    assertNotNull(result);
    assertEquals(expected, result.field);
}
```

---

## Estimated Effort

- **EmployeeService**: 6 tests, 2 hours
- **BudgetCheckService**: 6 tests, 2 hours
- **PurchaseRequestService**: 7 tests, 2.5 hours
- **AssetRequestService**: 6 tests, 1.5 hours

**Total**: ~20 tests, 8 hours

---

## Next: P1.5.2 Integration Tests

After contract tests complete:
- Integration tests (with real HTTP calls to endpoints)
- Test workflows: asset request lifecycle, budget check flow, cross-tenant isolation
- Estimated 4 hours, brings total tests to 160+

## Success Criteria

- [YES] All 20+ contract tests pass
- [YES] Mocks verify expected dependencies are called
- [YES] Tests document service contracts clearly
- [YES] Ready for P1.5.2 integration tests
