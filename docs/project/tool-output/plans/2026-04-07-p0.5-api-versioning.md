# P0.5 API Versioning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Change the application context path from `/api` to `/api/v1` and update all documentation to reflect the new endpoint paths.

**Architecture:** Spring Boot's configuration-only approach: modify `server.servlet.context-path` in application.yml, update Swagger OAuth2 redirect URL, and update all documentation files with new endpoint URLs. No controller code changes needed — Spring handles routing transparently.

**Tech Stack:** Spring Boot configuration, YAML, Markdown documentation

---

## File Structure

| File | Action | Responsibility |
|------|--------|-----------------|
| `services/business/src/main/resources/application.yml` | Modify | Change context-path and oauth2-redirect-url |
| `docs/WERKFLOW_INTEGRATION.md` | Modify | Update hardcoded endpoint URLs and examples |
| `docs/Independence-Checklist.md` | Modify | Update curl command examples |
| `Implementation-Summary.md` | Modify | Update example URLs in quick-start |

---

## Tasks

### Task 1: Update application.yml context-path

**Files:**
- Modify: `services/business/src/main/resources/application.yml` (lines 60-92)

- [ ] **Step 1: Read current application.yml to see the structure**

Run:
```bash
cat services/business/src/main/resources/application.yml | grep -A 30 "^server:"
```

Expected: Output shows current `context-path: /api` and swagger config.

- [ ] **Step 2: Update context-path in application.yml**

Find the line:
```yaml
server:
  port: ${SERVER_PORT:8084}
  servlet:
    context-path: /api
```

Replace with:
```yaml
server:
  port: ${SERVER_PORT:8084}
  servlet:
    context-path: /api/v1
```

- [ ] **Step 3: Update oauth2-redirect-url in Swagger config**

Find the line:
```yaml
    oauth2-redirect-url: http://localhost:8084/swagger-ui/oauth2-redirect.html
```

Replace with:
```yaml
    oauth2-redirect-url: http://localhost:8084/api/v1/swagger-ui/oauth2-redirect.html
```

- [ ] **Step 4: Verify the changes**

Run:
```bash
cat services/business/src/main/resources/application.yml | grep -A 5 "context-path\|oauth2-redirect-url"
```

Expected: Shows `context-path: /api/v1` and redirect URL with `/api/v1/`.

- [ ] **Step 5: Compile to verify no YAML syntax errors**

Run:
```bash
cd services/business
mvn clean compile -q
```

Expected: BUILD SUCCESS, no errors.

- [ ] **Step 6: Commit**

```bash
git add services/business/src/main/resources/application.yml
git commit -m "feat(P0.5.1): change context-path from /api to /api/v1"
```

---

### Task 2: Update WERKFLOW_INTEGRATION.md documentation

**Files:**
- Modify: `docs/WERKFLOW_INTEGRATION.md` (all endpoint references)

- [ ] **Step 1: Find all occurrences of `/api/` endpoints in the file**

Run:
```bash
grep -n "http://localhost:8084/api/" docs/WERKFLOW_INTEGRATION.md
```

Expected: Shows line numbers of endpoints like `/api/inventory/asset-requests`, `/api/purchase-requests`.

- [ ] **Step 2: Replace all `/api/` with `/api/v1/` using sed**

Run:
```bash
sed -i 's|http://localhost:8084/api/|http://localhost:8084/api/v1/|g' docs/WERKFLOW_INTEGRATION.md
```

Expected: Command completes without error.

- [ ] **Step 3: Verify the changes**

Run:
```bash
grep "http://localhost:8084/api/" docs/WERKFLOW_INTEGRATION.md | head -5
```

Expected: All matches show `http://localhost:8084/api/v1/`.

- [ ] **Step 4: Commit**

```bash
git add docs/WERKFLOW_INTEGRATION.md
git commit -m "docs(P0.5.2): update WERKFLOW_INTEGRATION.md to use /api/v1 endpoints"
```

---

### Task 3: Update Independence-Checklist.md documentation

**Files:**
- Modify: `docs/Independence-Checklist.md` (all endpoint references)

- [ ] **Step 1: Find all occurrences of `/api/` endpoints**

Run:
```bash
grep -n "http://localhost:8084/api/" docs/Independence-Checklist.md
```

Expected: Shows line numbers with curl commands and endpoint examples.

- [ ] **Step 2: Replace all `/api/` with `/api/v1/` using sed**

Run:
```bash
sed -i 's|http://localhost:8084/api/|http://localhost:8084/api/v1/|g' docs/Independence-Checklist.md
```

Expected: Command completes without error.

- [ ] **Step 3: Verify the changes**

Run:
```bash
grep "http://localhost:8084/api/" docs/Independence-Checklist.md | head -5
```

Expected: All matches show `http://localhost:8084/api/v1/`.

- [ ] **Step 4: Commit**

```bash
git add docs/Independence-Checklist.md
git commit -m "docs(P0.5.2): update Independence-Checklist.md to use /api/v1 endpoints"
```

---

### Task 4: Update Implementation-Summary.md documentation

**Files:**
- Modify: `Implementation-Summary.md` (endpoint references in quick start)

- [ ] **Step 1: Find all occurrences of `/api/` endpoints**

Run:
```bash
grep -n "http://localhost:8084/api/" Implementation-Summary.md
```

Expected: Shows line numbers with example URLs.

- [ ] **Step 2: Replace all `/api/` with `/api/v1/` using sed**

Run:
```bash
sed -i 's|http://localhost:8084/api/|http://localhost:8084/api/v1/|g' Implementation-Summary.md
```

Expected: Command completes without error.

- [ ] **Step 3: Verify the changes**

Run:
```bash
grep "http://localhost:8084/api/" Implementation-Summary.md
```

Expected: All matches show `http://localhost:8084/api/v1/`.

- [ ] **Step 4: Commit**

```bash
git add Implementation-Summary.md
git commit -m "docs(P0.5.2): update Implementation-Summary.md to use /api/v1 endpoints"
```

---

### Task 5: Run full test suite to verify no regressions

**Files:**
- None to modify; verification only

- [ ] **Step 1: Run all unit tests**

Run:
```bash
cd services/business
mvn clean test
```

Expected: All tests PASS. No failures or errors.

- [ ] **Step 2: Run full build and integration tests**

Run:
```bash
cd services/business
mvn verify
```

Expected: BUILD SUCCESS. All tests passing, no regressions.

- [ ] **Step 3: Verify Swagger is accessible at new path (manual or curl)**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:8084/api/v1/swagger-ui.html
```

Expected: Returns 200 (if service is running). If service not running, that's OK — just verifies the path is valid.

- [ ] **Step 4: Commit (if any test fixes were needed)**

If test code was modified to fix hardcoded URLs:
```bash
git add services/business/src/test/
git commit -m "test(P0.5.3): update test URLs to use /api/v1 endpoints"
```

If no test changes were needed:
```bash
git commit --allow-empty -m "test(P0.5.3): verify full test suite passes with /api/v1 context-path"
```

---

### Task 6: Final verification and ROADMAP update

**Files:**
- Modify: `Roadmap.md` (mark P0.5 complete)

- [ ] **Step 1: Verify all changes are committed**

Run:
```bash
git log --oneline -10
```

Expected: Shows commits from Tasks 1-5 (application.yml, WERKFLOW_INTEGRATION.md, Independence-Checklist.md, Implementation-Summary.md, test verification).

- [ ] **Step 2: Update Roadmap.md to mark P0.5 as complete**

Find the P0.5 section in `Roadmap.md`:
```markdown
#### P0.5 — API Versioning (/api/v1)
- [ ] **P0.5.1** Update application.yml context-path to include version
- [ ] **P0.5.2** Update all integration points to use /api/v1
```

Replace with:
```markdown
#### P0.5 — API Versioning (/api/v1)
- [x] **P0.5.1** Update application.yml context-path to include version *(commit: <SHA>)*
  - [x] Change: `server.servlet.context-path: /api`  (calls) `/api/v1`
  - [x] Update Swagger oauth2-redirect-url to `/api/v1/swagger-ui/oauth2-redirect.html`
  - [x] Compilation verified, no errors
  - [x] Completed: ~15 minutes

- [x] **P0.5.2** Update all integration points to use /api/v1 *(commit: <SHA>)*
  - [x] Update `WERKFLOW_INTEGRATION.md` endpoints
  - [x] Update `Independence-Checklist.md` curl examples
  - [x] Update `Implementation-Summary.md` URLs
  - [x] Full test suite passes (no regressions)
  - [x] Completed: ~30 minutes
```

(Use the actual commit SHAs from your git log.)

- [ ] **Step 3: Update "Current Session State" in Roadmap.md**

Find:
```markdown
## Current Session State

**Status**: P0.1-P0.4 COMPLETE [DONE] — Multi-tenancy, idempotency, processInstanceId, and cross-domain FK validation ready
**Active Phase**: P0 — Critical Path to Production (Weeks 1-2)
**Next Phase**: P0.5 — API Versioning (/api/v1)
```

Replace with:
```markdown
## Current Session State

**Status**: P0.1-P0.5 COMPLETE [DONE] — Multi-tenancy, idempotency, processInstanceId, FK validation, and API versioning ready
**Active Phase**: P0 — Critical Path to Production (Weeks 1-2)
**Next Phase**: P0.6 — Pagination on List Endpoints
**Last Commit**: P0.5 API versioning complete
**Branch**: feature/p0-multi-tenancy
```

- [ ] **Step 4: Update "Project Health" section in Roadmap.md**

Find:
```markdown
**Current State**: werkflow-erp extracted as standalone service, all CRUD APIs implemented with P0.1-P0.4 complete:
- [YES] Multi-tenant scoping (COMPLETE — P0.1)
- [YES] Idempotency for safe retries (COMPLETE — P0.2)
- [YES] Cross-domain FK validation (COMPLETE — P0.4)
- Remaining for MVP: API versioning, Pagination on list endpoints
```

Replace with:
```markdown
**Current State**: werkflow-erp extracted as standalone service, all CRUD APIs implemented with P0.1-P0.5 complete:
- [YES] Multi-tenant scoping (COMPLETE — P0.1)
- [YES] Idempotency for safe retries (COMPLETE — P0.2)
- [YES] Cross-domain FK validation (COMPLETE — P0.4)
- [YES] API versioning (/api/v1) (COMPLETE — P0.5)
- Remaining for MVP: Pagination on list endpoints
```

- [ ] **Step 5: Commit Roadmap updates**

```bash
git add Roadmap.md
git commit -m "docs(P0.5): mark P0.5 API versioning complete in ROADMAP"
```

- [ ] **Step 6: Final verification**

Run:
```bash
git log --oneline -10
```

Expected: Shows all P0.5 commits plus the Roadmap update.
